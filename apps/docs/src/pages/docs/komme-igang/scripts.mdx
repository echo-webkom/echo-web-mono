import { Callout } from "nextra/components";

# Scripts

Vi har noen scripts som gjør det enklere å kjøre kommandoer.

## `pnpm`

Vi bruker [`pnpm`](https://pnpm.io/) som pakkebehandler (package manager). Grunnen til dette er at pnpm raskere enn `npm` og `yarn`, og har en del andre fordeler. Du kan lese mer om det på [nettsiden deres](https://pnpm.io/).

Pakkebehandlere lar oss laste ned ekstern kode som vi kan bruke i prosjektet vårt. Dette gjør det enklere å gjenbruke kode, og gjør at vi ikke trenger å skrive alt fra bunnen av.

## `pnpm [web:|docs:|cms:|db:]dev`

Dette scriptet starter opp utviklingsmiljøet for web, dokumentasjon, Sanity Studio, Drizzle Studio eller alle sammen.

For å starte alle samtidig kjør `pnpm dev`.

Om man ønsker å starte nettsiden bruker man `pnpm web:dev`.

```sh
pnpm dev
pnpm web:dev
pnpm docs:dev
pnpm cms:dev
pnpm db:dev
```

## `pnpm db:{up|down}`

Denne kommandoen starter (bygger) eller stopper (ødelegger) databasen. Databasen er en postgres database som kjører i en docker container.

```sh
pnpm db:up
pnpm db:down
```

<Callout>
  Husk å initialisere databasen etter at du har startet den. Dette gjøres med `pnpm db:deploy`.
</Callout>

## `pnpm db:generate`

Dette scriptet genererer migrasjonsfilene til Drizzle.

```sh
pnpm db:generate
```

## `pnpm db:migrate`

Dette scriptet lager ny migrasjon. Her må du også kjøre databasen først.

```sh
pnpm db:migrate
```

## `pnpm lint`

Denne kommandoen kjører linting på hele prosjektet. Den vil ikke fikse problemene. Linting kan hjelpe deg med å skrive bedre kode, og er en viktig del av prosjektet.

```sh
pnpm lint
```

## `pnpm lint:fix`

Scriptet fikser linting problemer. Om `pnpm lint` finner problemer, kan du kjøre dette scriptet for å fikse dem.

```sh
pnpm lint:fix
```

## `pnpm format`

Denne kommandoen formatterer koden etter reglene vi har satt i `prettier.config.cjs`.

```sh
pnpm format
```

## `pnpm format:check`

Denne kommandoen sjekker om koden er formattert etter reglene vi har satt i `prettier.config.cjs`, og vil ikke fikse de automatisk.

```sh
pnpm format:check
```

## `pnpm typecheck`

Kjører typescript sin kompilator uten å faktisk kompilere koden. Denne vil sjekke om alle typene i koden er skrevet riktig, og om det er noen feil i koden.

```sh
pnpm typecheck
```
