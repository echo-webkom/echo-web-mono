{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "tsconfig/base.json",

  "compilerOptions": {
    "allowJs": true
  },

  "include": [".eslintrc.cjs", "*.ts", "src", "schemas"],
  "exclude": ["node_modules", "dist", ".sanity", "migrations"]
}
