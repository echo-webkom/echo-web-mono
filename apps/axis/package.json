{
  "name": "@echo-webkom/axis",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "pnpm with-env go run cmd/main.go",
    "build": "pnpm with-env go build -o build/axis cmd/main.go",
    "test": "pnpm with-env go test ./... -v",
    "with-env": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "dotenv-cli": "7.4.4"
  }
}
